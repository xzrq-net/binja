---
tier: nit
---
# Include the resume command in the analysis-hold error

Observed 2026-09-13 by a cold trial subject (Fable) during the guide
compression review: a typed command against a view with analysis on hold fails
with an error that says to resume explicitly with `py --allow-incomplete` but
does not give the command. The guide carries the one-liner
(`bv.set_analysis_hold(False); bv.update_analysis_and_wait()`), and the subject
named it as one of the few guide lines they could not have done without.

If the error printed the full command, the guide line could go.

Closed: Hold error prints a shell-quoted resume command with --state-dir and the resolved --target; guide one-liner removed.
