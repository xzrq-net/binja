---
tier: nit
---
# Condense the typed analysis commands section of docs/design.md

Each milestone part (5bkxzv, 2wrz2e, fm4yyw) appended its own paragraphs with
per-command JSON row schemas to the "Typed analysis commands" section. Once the
zbqnx2 milestone closes, rewrite the section as one contract statement (request
kinds, resolver rules, page shape, text/JSON split) plus a compact table of
commands and row fields, so later parts cite it instead of extending it.

Closed: Consolidated the typed analysis commands section into shared rules and a command schema table, preserving its behavioral constraints and leaving the rest of design.md unchanged.
