---
tier: objective
---
# Refine CLI defaults and on-demand agent guidance

Incorporate the user's design refinements: “static analysis only”; “default posture is to only operate once analysis is finished”; “start with binary ninja's natural representations”; and “binja --help” leading to “binja skill”. The user authorized subagent usability trials after the initial implementation works.

Update the design and existing implementation tasks without starting runtime implementation. Keep specialized loading, extra Python dependencies, generalized undo, and extensive GUI automation deferred. Preserve uncertainty about save cadence while making deliberate slow saves usable and discoverable. Move the separate-workspace CLI access check to the first working RPC milestone.

Verify the documentation and deeds graph, then commit the refinement locally.

Closed: Updated the design and existing tasks with static-analysis scope, readiness defaults, natural address conventions, save guidance, on-demand CLI guide discovery, and authorized usability trials. Corrected and verified the packaging archive hash.
