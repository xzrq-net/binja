---
blocked-by: [2nbgtk]
---
# Extend API lookup with member listing and runtime introspection

Build on MVP `api search`/`api show` using observed lookup failures from usability trials.

- Add `api members`, treating inherited members and ambiguous symbols accurately.
- Supplement static docs with explicit runtime introspection for extension classes when needed. Never evaluate every property during enumeration.
- Keep ordinary static lookup independent of GUI startup, license injection, and CLI imports of Binary Ninja. Report version/source and limitations.

Verify a large class, inherited members, missing/ambiguous symbols, and extension-class behavior. Update the packaged guide from concrete mistakes; do not create another API manual.
